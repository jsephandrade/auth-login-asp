# Option 1: full connection string (recommended)
AUTH_DB_CONNECTION=server=127.0.0.1;port=3306;database=auth-login-asp;user=aoshan;password=pransocool

# Option 2: keep appsettings connection string and only inject password
# MYSQL_ROOT_PASSWORD=REPLACE_WITH_REAL_PASSWORD
